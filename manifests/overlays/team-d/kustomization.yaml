apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: team-d

resources:
  - ../../base
  - resource-controls.yaml

patches:
  - path: web-ingress-patch.yaml
    target:
      kind: Ingress
      name: web
