apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app.kubernetes.io/name: db
    training.k8rvis.io/security-baseline: insecure
spec:
  selector:
    app.kubernetes.io/name: db
  ports:
    - name: redis
      port: 6379
      targetPort: redis
